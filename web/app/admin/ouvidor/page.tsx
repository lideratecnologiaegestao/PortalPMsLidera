'use client';

import ManifestacoesAdmin from '../_components/ManifestacoesAdmin';

/** Painel do Ouvidor: caixa única dos dois canais (Ouvidoria + e-SIC). */
export default function PainelOuvidorPage() {
  return <ManifestacoesAdmin />;
}
